using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SpaceTrash : SpaceJunk
{
    // Start is called before the first frame update
    void Start()
    {
        SetScale();
        SetRotation();
    }
}
